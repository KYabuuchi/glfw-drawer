cmake_minimum_required(VERSION 3.0)
find_package(GLEW REQUIRED)
if (GLEW_FOUND)
    message("HOGE")
    include_directories(${GLEW_INCLUDE_DIRS})
    link_libraries(${GLEW_LIBRARIES})
endif()
add_executable(main main.cpp)
target_link_libraries(main GL glfw GLEW)
